PROJECT: Kas Kita — aplikasi manajemen kas kelas berbasis web (PWA)
TECH STACK: React (Frontend) + Spring Boot (Backend) + Firebase (Auth, Firestore, Storage, FCM)
BASE PACKAGE: com.kaskita
API BASE URL: https://api.kaskita.id/api/v1
AUTH: Firebase ID Token (Bearer) — role dibaca dari Firestore, BUKAN dari JWT claims

[CONTEXT BLOCK DI ATAS]

STEP: B4 — Authentication Middleware

CONTEXT SEBELUMNYA: Firebase sudah terinisialisasi (Step B3 selesai).

TASK:
Implementasi JWT authentication menggunakan Firebase ID Token:
1. FirebaseTokenFilter.java (extends OncePerRequestFilter) — validasi Bearer token dari request header
2. SecurityConfig.java — konfigurasi Spring Security: gunakan filter, disable session, /auth/** publik
3. FirebaseTokenUtil.java di util/ — helper untuk verify token dan extract uid

ATURAN:
- Role TIDAK dibaca dari JWT claims
- Role akan di-fetch dari Firestore users collection (diimplementasi di step berikutnya)
- Return 401 jika token tidak ada atau tidak valid

BATASAN:
- Jangan implementasi Firestore role fetching dulu
- Hanya token validation saja

cek apakah sudah ada apa belum